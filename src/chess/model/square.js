class Square{
    constructor(x,y,canvasCoordinates,piece){
        // X-Y COORDINATES
        this.x=x;
        this.y=y;

        // IN Pixels
        this.canvasCoordinates=canvasCoordinates;

        // Chess Piece
        this.piece=piece; 
    }
}